services:
  web:
    build: .
    ports:
      - "8081:80"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_API_KEY=${VITE_API_KEY}
      - VITE_API_URL=${VITE_API_URL}
